![Alt text](./rating.svg)

# REST-API film library management service

To get started run:

1). `sudo docker compose up --build` or `make up_build`

2). `sudo docker compose up -d` or `make up_d`

3). `sudo docker exec -it flask flask seed_all` or `make seed`

Url with swagger documentation — http://localhost/api/
